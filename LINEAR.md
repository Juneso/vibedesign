# Linear 이슈 작성 및 관리 규칙

> DES-224 기반 | SKT-128 포맷 강제 반영 | JSN-162 명사형 종결 반영 | JSN-163 콜랩스 줄바꿈·결과 상세 | JSN-166 목표·결과 체크박스 조응
>
> **버전 추적 규칙**: 이 지침을 수정할 때는 반드시 근거가 된 Linear 이슈를 만들고(또는 찾고), 위 헤더에 이슈 번호를 병기한다. "왜 이 규칙이 됐는지"는 해당 이슈에서 추적한다.

## 이슈 제목

**핵심 원칙: 목적을 포함하되, 최대한 짧은 명사구**

- 무엇을 했는지 (X) → 왜 필요한지 + 무엇을 하는지 (O)
- **명사구로 마무리** 선호 ("~세팅", "~구축", "~다각화") — 긴 서술형 ("~할 수 있도록") 지양
- 짧게 줄여도 의미 전달이 비슷하면 무조건 짧게
- **프로젝트 내 통일 어휘 사용** — 같은 개념은 항상 같은 단어로. 지엽적인 기술 용어는 제목에 넣지 말 것

```
❌ "CLAUDE.md 계층 구조 설계"              (무엇을 했는지만)
✅ "어느 맥에서든 클로드 지침 일관 적용"    (목적 + 명사구)

❌ "유저 프로필 기반 derivedKeywords 생성 파이프라인 추가"  (지엽 기술 용어)
✅ "프로필 기반 대화가 자연스럽도록 키워드 다각화"          (통일 어휘 + 명사구)

❌ "프롬프트 바꿀 때마다 5분 안에 품질 변화를 확인할 수 있도록"  (너무 긺)
✅ "클로드가 채점 후 사용자가 검수하는 플로우 구축"              (행위자 + 명사구)
```

**프로젝트 통일 어휘 (독서 도우미 기준)**
- 사용자 인풋 / 맥락 / 유저 프로필 → **프로필**
- 메모 → wiki 변환 → **인제스트(Ingest)**
- 질문 추천 → **넛지(Nudge)**
- 지식 저장소 → **위키(Wiki)**
- AI 품질 평가 → **채점 / 합격률**

> 새 프로젝트에서는 해당 프로젝트의 통일 어휘를 먼저 파악하고 제목에 일관 적용.

## 이슈 본문 구조 — 포맷 강제

**핵심 규칙 (위반 시 작성 금지):**

1. **섹션당 불렛 최대 3줄.** 배경·목표·작업·결과 각 섹션은 불렛 3줄 이내. 그 세 줄만 읽으면 비개발자도 해당 섹션을 이해할 수 있어야 한다.
2. **불렛(=콜랩스 제목)은 비개발자 언어로.** 개발 용어(파일 경로, API 이름, 라이브러리 등)는 보이는 줄에 쓰지 않는다.
3. **상세가 필요한 불렛은 그 불렛 자체를 콜랩스로.** 별도의 "상세" 불렛을 새로 만들지 않는다. 3개 불렛 중 더 깊이 설명할 게 있는 불렛을 Linear 콜랩스(`+++`)로 바꿔, **콜랩스 제목 = 자연어 불렛 내용**, **접힌 내용 = 전문용어 포함 상세 설명**으로 작성한다.
4. 불렛 3줄로 요약이 안 되면 → 이슈가 너무 크다는 신호. 서브이슈로 분해한다.
5. **문장은 명사형으로 끝내고, 그 끝에 마침표를 찍지 않는다.** 서술형 종결(`~한다`·`~된다`·`~합니다`·`~하세요`)을 쓰지 않는다. 단어로 끊거나 `~임`·`~했음`·`~필요`·`~완료` 같은 명사형으로 맺는다. 문장 끝의 꼬리가 사라져 **같은 3줄에 더 많은 정보가 들어간다.**
6. **콜랩스 안 상세는 불렛으로 쓴다.** 문장(항목) 하나가 불렛 하나 — 문장이 끝나면 다음 문장은 새 불렛으로 줄을 바꾼다. 묶음이 필요하면 `**소제목**` 한 줄 아래 불렛들을 둔다. 여러 문장을 한 문단으로 뭉치면 펼쳐도 훑어읽을 수 없다 — 콜랩스는 접어서 숨기는 장치지, 뭉쳐 쓰는 면죄부가 아니다.

```
❌ 이슈 본문은 항상 현재 기획을 반영해야 한다.
❌ 이슈 본문은 항상 현재 기획을 반영.        ← 명사형인데 마침표가 남음
✅ 이슈 본문은 항상 현재 기획을 반영

❌ 접근 권한과 키 발급은 BKT-384에서 끝난다.
✅ 접근 권한·키 발급은 BKT-384 에서 완료됨

❌ 두 맥이 같은 서식과 같은 테스트 결과를 냅니다.
✅ 두 맥이 같은 서식·같은 테스트 결과를 냄

❌ 설정 파일을 임의로 바꾸지 마세요.
✅ 설정 파일 임의 변경 금지
```

**마침표 규칙 — 붙었다 안 붙었다 하는 게 가장 지저분하다:**

- 한 문장짜리 불렛·문단 → 마침표 **없음**
- 문장이 여럿인 문단 → 문장 **사이만** `.` 으로 끊고, **마지막 문장은 마침표 없이** 끝냄
- `?` · `:` · `—` 는 그대로 사용

```
✅ 커밋 `1c5b89a` 로 반영 완료

✅ 두 파일은 공통부가 같음. 차이는 레포 전용 섹션 하나뿐임
   (앞 문장 뒤엔 마침표, 마지막 문장 뒤엔 없음)
```

> **적용 범위**: 이슈 **본문과 댓글** — 콜랩스 안 상세 설명까지 포함
> **예외 둘**: ① 코드 블록·인용문 안의 원문(붙여넣기용 프롬프트, 로그, 명령어)은 손대지 않는다.
> ② 이 지침 문서 자체는 해당 없음 — 규칙이 적용되는 대상은 리니어에 쓰는 글이다

```
## 배경
- 평범한 불렛 (상세가 필요 없으면 그냥 둠)
+++ 더 설명이 필요한 불렛 내용을 자연어로 (이 줄이 접힌 상태에서 보임)
여기에 기술 용어, 파일 경로, 의사결정 디테일 등 상세 설명
+++
- (배경 전체 최대 3줄)

## 목표
- 이 이슈가 끝나면 무엇이 가능해지는가 (최대 3줄)

## 작업
+++ 무엇을 할 것인가 — 비개발자 언어로 (이 줄이 제목)
구체적 구현 단계, 기술 스택, 경로 등 상세
+++
- 상세가 필요 없는 작업은 그냥 불렛으로 (전체 최대 3줄)

## 결과
- 완료 후 실제로 달라진 것 (최대 3줄 — 완료 시 채움)
+++ 상세가 필요한 결과 불렛은 콜랩스로
- 산출물(파일·커밋) · 설계 이유 · 동작 방식 · 한계·후속 — 항목마다 불렛
+++
```

> 핵심: 콜랩스는 **불렛을 대체**하는 것이지 추가하는 게 아니다. 접힌 상태로 봤을 때 제목(=자연어 불렛)만으로 의미가 통해야 하고, 펼치면 전문용어 상세가 나온다.

## 본문은 기획의 SSOT

- 이슈 본문은 항상 **"현재 기획"** 을 반영한다. 방향이 바뀌면 **본문 자체를 수정**한다. 댓글에만 로그를 남기고 본문을 방치하는 것 금지.
- 역할 분리: **본문 = 지금 무엇인가(단일 진실)** / **댓글 = 언제·왜 바뀌었는가(변경 로그)**.
- 본문에 변경 로그를 누적하지 않는다 (`## 방향 변경 로그` 같은 로그 섹션 금지). 이력은 댓글로, 결과는 본문 수정으로.

## 목표·결과 조응 — 목표는 체크리스트, 결과는 부연

- `## 목표`는 이 이슈를 끝내는 데 필요한 **마이크로 이슈**들을 체크박스(`- [ ]`)로 나열한다. 서브이슈로 뺄 만큼 크지 않은 실행 단위 하나 = 체크박스 하나. **진행되면 목표의 체크박스를 `- [x]`로 직접 갱신** — 진행 상황은 목표에서 읽는다.
- `## 결과`는 기존 방식(불렛 + 콜랩스) 그대로 쓰되, **목표 항목에 조응하는 부연설명**으로 쓴다 — 어느 체크박스가 어떻게 끝났는지(산출물·설계 이유·동작·한계), 안 끝난 것은 왜 남았는지.
- 목표와 결과에 같은 목록을 반복하지 않는다 — **목록은 목표에만, 서사는 결과에만.**
- 진행 중 새 일이 생기면 목표에 체크박스를 추가한다. 목표 체크박스 목록은 "섹션당 불렛 3줄" 제한의 예외 — 대신 한 줄씩 짧게.
- 목표·결과 섹션 제목 아래에 규칙 안내 문구("체크박스 하나 = 마이크로 이슈", "결과와 조응" 등)를 넣지 않는다 — 내용만 쓴다.

## 이슈 책임 관리 원칙

### 완료 시
- 이슈 상태를 Done으로 변경
- 본문 `## 결과` 섹션에 실제 결과 기록 (불렛 3줄, 상세가 필요한 불렛은 콜랩스로)
- **결과 콜랩스는 "다시 물어볼 필요가 없는" 상세도로** (JSN-163) — 구현 직후 대화로 보고하는 수준. 네 가지를 담는다:
  - **산출물**: 바뀐 파일·커밋과 각각이 하는 일
  - **설계 이유**: 왜 그 방식인지, 대안 대비 무엇을 얻었는지
  - **동작 방식**: 유저가 실제로 보게 되는 것, 데이터가 흐르는 경로
  - **한계·후속**: 안 한 것, 미뤄진 것, 알려진 제약

### 방향이 바뀌었을 때
- **본문(배경·목표·작업)을 현재 기획에 맞게 직접 수정** — 본문은 SSOT
- 변경 이력은 댓글로 기록: 언제, 왜 바뀌었는지
- 충돌하는 기존 이슈가 있으면 해당 이슈에도 댓글로 영향 명시

### 새 이슈 생성 시 — 필수 체크리스트

**이슈 생성 전 아래 순서 반드시 실행 (추측 금지):**

1. `list_milestones` 호출 → 관련 마일스톤 확인
2. `list_issues` 호출 → 해당 마일스톤의 **기존 이슈 전체** 확인
3. 아래 판단 트리 실행:
   - 이 작업이 기존 이슈의 **연장·후속·잔여 작업**인가? → **서브이슈**로 생성 (`parentId` 필수)
   - 기존 이슈와 **동일한 목표**를 가지는가? → 새 이슈 대신 **기존 이슈 업데이트** 우선
   - 위 둘 다 해당 없는 **독립적인 새 작업**인가? → 새 이슈 생성
4. **분해 점검 (본문 작성 후, save_issue 호출 전 필수)**: 작성한 본문의 `## 작업`에 작업 단위가 2개 이상인가?
   - YES → 부모 본문에는 배경·목표·**서브이슈 링크 목록**만 남기고, 각 작업을 서브이슈로 생성 (`parentId` 필수)
   - 작업 상세를 부모 본문에 몰아넣은 채 생성 종료 금지
5. **포맷 점검 (save_issue 호출 전 필수)**: 각 섹션 불렛 3줄 이하인가? 접힌 상태(불렛·콜랩스 제목)에 개발 용어가 없는가? 상세는 콜랩스 안에만 있는가? 비개발자가 접힌 상태만 읽고 이해 가능한가? **모든 문장이 명사형으로 끝나는가(위 포맷 강제 5번)? 콜랩스 안이 문장마다 불렛으로 쪼개져 있는가(6번)? 목표가 체크박스(마이크로 이슈)로 나뉘어 있고 결과가 그 항목들의 부연으로 조응하는가(목표·결과 조응 규칙)?** — 하나라도 NO면 본문 재작성
6. `save_issue` 호출 시 아래 필드 **모두 포함**:
   - `team` — 팀 이름
   - `project` — 프로젝트 이름
   - `milestone` — list_milestones 결과에서 확인한 정확한 이름
   - `assignee` — 담당자
   - `parentId` — 서브이슈인 경우 부모 이슈 ID (누락 금지)

**누락 허용 안 됨**: project / milestone 빠진 이슈, 서브이슈인데 parentId 없는 이슈 생성 금지.

```
판단 예시:
"DES-294 작업 중 남은 맥 1대에 동일 작업 필요" → DES-294의 서브이슈
"DES-294와 별개로 새로운 동기화 방식 실험" → 새 이슈
```

- 충돌하는 이슈가 있으면 `blockedBy` 또는 `relatedTo` 관계 설정

### 수정 시 — 본문 수정 + 댓글 로그
- 변경사항이 생기면 **본문을 먼저 수정**하고, 댓글로 변경 이유를 기록
- "언제 무엇을 왜 바꿨는지"는 댓글에서, "지금 무엇인지"는 본문에서 추적 가능해야 함

## 이슈 마일스톤 배정

이슈 생성 전 반드시:
1. `list_milestones`로 해당 프로젝트 마일스톤 목록 읽기
2. 이슈 목적이 어느 마일스톤 달성에 기여하는지 판단
3. 적합한 마일스톤 없으면 새 마일스톤 제안

판단 기준: "이 이슈가 완료되면 어느 마일스톤이 한 걸음 진전되는가?"

## 프로젝트 시작 시 이슈 생성

새 프로젝트 시작할 때 클로드가 해야 할 것:
1. 마일스톤 생성 (프로젝트 목표 — 왜 이 마일스톤이 필요한지 description에 기록)
2. 주요 작업 단위별 이슈 생성 (제목은 "왜 필요한지" 원칙 적용)
3. 첫 이슈를 In Progress로 설정

## 대화 중 의사결정 기록

작업 중 대화에서 중요한 의사결정이 나오면 즉시 리니어에 반영:
- **방향 전환**: 본문(배경·목표·작업) 직접 수정 + 댓글로 변경 이유 기록
- **준서의 인사이트·깨달음**: 관련 이슈 댓글로 기록 ("인사이트: ...")
- **완료된 작업**: 이슈 Done + `## 결과` 섹션 채우기

기준: "나중에 왜 이렇게 됐는지 알 수 있어야 한다"

## 서브이슈

큰 이슈는 서브이슈로 분해. 서브이슈가 모두 Done이어야 부모 이슈 Done 처리.

**기본 규칙: 작업 단위가 2개 이상이면 한 이슈에 몰아넣지 말고 반드시 서브이슈로 쪼갠다. (이슈 생성 시 필수 점검 단계)**

**서브이슈 제목 원칙: "왜 이 서브이슈가 부모 이슈 해결에 필요한지"에 초점**

```
부모: "하루 마무리 회고와 지식 축적이 자동으로 이어지도록"

❌ "회고 포맷 정의"           (무엇을 했는지)
✅ "회고에서 의사결정이 빠지지 않도록 포맷 구조화"  (왜 필요한지)

❌ "wiki ingest 연결"
✅ "회고가 끝나면 위키에 자동 반영되도록"
```

## 플로우 구조 (Release 프로젝트)

Release 프로젝트는 이슈를 **3계층 트리**로 관리한다. 상세 SSOT: [`docs/flow-structure.md`](docs/flow-structure.md).

- `N | 플로우명` (최상위 플로우) → `N-M | 핵심 기획`(`기획` 라벨) → 구현 서브이슈(`UX`/`인프라` 라벨)
- **결정(기획)이 위, 구현(UX·인프라)이 아래.**
- **최상위 플로우(`N | …`) 본문엔 항상 `## 구조 (한눈에)` 구조도가 있어야 하고, 기획이 바뀌면 갱신한다.**
  - 구조도는 본문에 **머메이드 코드 블록**(` ```mermaid `)으로 직접 넣는다 (JSN-166 후속 결정, 0901)
  - 이미지 업로드 방식(`scripts/flow-diagram.mjs` SVG→PNG→업로드)은 폐기 — 코드 블록이라 갱신이 본문 편집으로 끝남. 스킬 `.claude/skills/flow-diagram` 은 참고용으로만 남김

## 프로젝트 / 팀 배정
| 성격 | 팀 | 프로젝트 |
|------|-----|---------|
| 개인 작업 (PKM, 독서, 생산성) | Junseo | 해당 프로젝트 |
| SKT 업무 | SKT | 해당 프로젝트 |
