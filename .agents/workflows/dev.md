---
description: 로컬 개발 서버를 시작합니다.
---
# /dev 로컬 서버 구동 워크플로우

// turbo
1. 터미널에서 백그라운드로 `lsof -i :5173 -t | xargs kill -9 2>/dev/null || true && npm run dev` 명령을 실행합니다. (이 명령어는 이전에 열려있는 서버 포트를 닫아 서버가 중복 실행되는 것을 방지합니다.)
2. 실행에 성공하면 브라우저 접속 주소(예: http://localhost:5173)를 사용자에게 안내합니다.
