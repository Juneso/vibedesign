# Motion Canvas — 프로토타이핑 작업 가이드

## 프로젝트 성격

**Figma 디자인 → HTML/CSS/JS 프로토타입 → 모션 미세조정 → 개발자 핸드오프**를 위한 실험 뷰어.
각 실험은 `src/experiments/active/<YYYY-MM-DD>_<name>/` 폴더 하나이고, 최종 산출물은 "이 수치/타이밍/인터랙션 그대로 구현해달라"고 개발자에게 넘기는 레퍼런스.

**중요**: 이건 제품 코드가 아니라 **명세서로서의 코드**. 가독성·정확한 수치·재현성이 최우선. 성능 최적화/추상화 욕심 내지 말 것.

## 디렉토리 구조

```
src/
  experiments/
    active/<실험폴더>/       # 작업 중
      index.html
      style.css
      config.js              # 애니메이션 타이밍/수치 상수 — 핸드오프 대상
      script.js              # 인터랙션 로직
      meta.json
    archive/                 # 완료/보관
    registry.js              # 뷰어에 노출되는 실험 목록
  shared/
    js/
      interactions.js        # 공통 entrance/exit, STT expand 등
      animation.js
      mobile-nav.js
      router.js
      theme.js
    styles/
      base.css
      shell.css              # 뷰어 쉘 (사이드바 등)
```

## 핵심 작업 원칙

### 1. 수치는 `config.js`에 — 하드코딩 금지
모션 수치(ms, stiffness, offset, easing)는 **무조건** 해당 실험의 `config.js`에 상수로 빼고 script.js에서 참조. 개발자가 핸드오프 후 값을 보기 위함.
- 예외: CSS 키프레임 내 값은 CSS에 둬도 OK. 단 주석으로 "config.js의 X와 연동" 표시.
- 새 수치를 추가할 땐 기존 섹션 구조(`stt`, `textGroup`, `gradientCard`, `interactions` 등)를 따를 것.

### 2. 공통 패턴은 `shared/js/interactions.js`로
텍스트/버튼 entrance·exit, STT expand 같은 재사용되는 인터랙션은 개별 실험에 복붙하지 말고 공통 모듈 사용. 2개 이상 실험에서 같은 패턴이 보이면 이관 제안.

### 3. "페이지 N처럼" 지시에 민감할 것
사용자가 "페이지 3의 fx-layer 방식처럼" "페이지 3 카드 섀도우를 토스트에" 식으로 참조함. 지시를 받으면 **먼저 해당 페이지 코드를 읽고** 동일 패턴·같은 수치·같은 셀렉터 구조로 맞출 것. 재구현 금지.

### 4. Figma 참조가 등장하면 Figma API 사용
색상·치수 관련 불명확함이 있으면 추측 반복 대신 Figma API(MCP 또는 제공된 토큰)로 값을 확인. 특히 그라데이션 정지점·conic 팔레트·쉐도우 파라미터.

### 5. 반응형은 모바일/데스크톱 명시적으로
레이아웃 변경 요청 시 "전체 적용"인지 "모바일만"인지 모호하면 먼저 확인. 기본 가정은 **데스크톱 변경은 데스크톱만, 모바일 변경은 `<=768px` 미디어 쿼리 안에**. 어제 top margin 이슈처럼 한 번에 잡을 것.

### 6. 실험 간 공유 자원 변경 시 영향 범위 확인
`shared/styles/shell.css`나 `shared/js/*.js` 변경은 모든 실험에 영향. 커밋 전에 "이게 다른 active 실험의 기존 동작을 깨지 않는지" 한 번 생각.

## 커밋 메시지 컨벤션

기존 스타일 따를 것:
- `fix(pageN): ...` / `feat(pageN): ...` — 특정 실험만
- `fix(all-pages): ...` — 모든 active 실험 공통
- `feat: ...` / `fix(UI): ...` — 공유 모듈/쉘
- `revert(pageN): ...` — 되돌림

범위 뒤 콜론, 소문자, 동사 원형. 수치 변경은 before→after를 본문에 남기면 좋음 (예: `stiffness 220→230`).

## 작업 진행 팁 (AI 용)

- **마이크로 튜닝 배치**: 사용자가 "색상 바꾸고, 타이밍 줄이고, 오프셋 +10px" 식으로 여러 개 요청하면 한 번에 적용 + 한 커밋. 단 시각 확인이 필요한 건 분리.
- **수치 프리뷰**: 수치 변경 후엔 before/after를 한 줄로 알려줄 것. "2.25s → 2.0s, dim opacity 0.6 → 0.5".
- **스피너/토스트 같은 시각 효과**: 픽셀·각도·색상 반복 조정이 많음. 한 턴에 여러 축(각도·색·타이밍)을 동시에 건드리지 말고, 사용자가 지정한 축만 건드릴 것. "ease 타이밍만 바꿔줘"에 색상 같이 건드리면 혼선.
- **개발자 핸드오프 관점**: 주석/네이밍이 정확해야 함. `gradientYIn: [30, 0]` 같은 튜플은 `[from, to]`라는 걸 주석이나 변수명으로 명시.
- **`.DS_Store` 커밋**: 맥 특성상 자주 변경됨. 스테이징 전에 필터링 권장 (`.gitignore` 미등록이라면 등록 제안).

## 지양할 것

- 제품 코드 수준의 추상화·일반화 (훅, 팩토리, 복잡한 타입)
- 요청 밖의 "김에 정리" (다른 실험 리팩토링, 미관 개선 등)
- 동작 검증 없는 "다 됐습니다" 응답. UI 변경은 수치 before/after + 어떤 파일을 건드렸는지 짧게 리포트.
- TypeScript/빌드 파이프라인 변경 제안 — 지금 구조는 vanilla JS + 직접 로드가 의도.
