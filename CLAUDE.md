# vibedesign 작업 지침

## 프로젝트 개요

React + Vite + Motion(Framer Motion) 기반의 디자인 프로토타입 실험 레포.  
독서 도우미, 인터랙션 프로토타입 등 날짜별 실험이 누적된다.  
Vercel로 배포되며, 알라딘 API + OpenAI API를 연결한 풀스택 실험도 포함.

**기술 스택**
- React 19 + Vite 7
- Motion (Framer Motion 12)
- Tailwind CSS 4
- Vercel (배포) / `api/` 폴더 → Vercel Serverless Functions
- `native-preview/` → Expo React Native (iOS 네이티브 미리보기용)

---

## 폴더 구조

```
vibedesign/
├── src/
│   ├── experiments/
│   │   └── active/
│   │       └── YYYY-MM-DD_이름/     # 실험 단위 폴더 (날짜-이름 형식)
│   │           ├── index.html       # 실험 진입점
│   │           ├── meta.json        # 실험 메타데이터 (제목, 설명 등)
│   │           ├── main.jsx / script.js
│   │           ├── style.css
│   │           ├── screens/         # React 화면 컴포넌트 (book-wiki-mvp 등)
│   │           ├── lib/             # 실험 전용 유틸/서비스
│   │           └── eval/            # LLM 품질 평가 스크립트 및 결과
│   ├── shared/
│   │   ├── styles/                  # 공통 CSS (base, tokens, device, shell)
│   │   ├── js/                      # 공통 JS 유틸 (animation, router, theme 등)
│   │   ├── plugins/                 # 공통 플러그인 (aladin, openai)
│   │   └── viewer/                  # 디자인 토큰 뷰어
│   └── registry.js                  # 실험 목록 등록
├── api/                             # Vercel Serverless Functions
│   ├── book-search.js
│   └── book-detail.js
├── docs/                            # 작업 가이드, 프로세스 문서
├── native-preview/                  # Expo 앱 (React Native)
├── .agents/workflows/               # Claude Code 에이전트 워크플로우
├── index.html                       # 실험 목록 진입점
├── vite.config.js
└── vercel.json
```

**새 실험 추가 시**: `src/experiments/active/YYYY-MM-DD_이름/` 폴더 생성 → `meta.json` 작성 → `src/registry.js`에 등록

---

## 커밋 컨벤션

```
<type>(<scope>): <설명>
```

**type**
| 타입 | 의미 |
|------|------|
| `feat` | 새 기능, 실험 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서만 변경 |
| `style` | CSS/스타일 변경 |
| `refactor` | 리팩터 (기능 변화 없음) |
| `eval` | LLM 평가 실행/결과 추가 |
| `chore` | 빌드, 설정, 의존성 |

**scope**: 실험 폴더 이름 또는 공통 영역 이름 (선택적으로 `/DES-123` Linear 이슈 번호 병기)

```
feat(book-wiki-mvp/DES-270): 넛지 V5/V6 변형 eval + seed 4책 확장
docs(book-wiki-mvp): Linear 룰 글로벌로 이동
fix(shared): router 뒤로가기 버그 수정
chore: vite 7 업그레이드
```

---

## 역할 분리 (OMC 차용)

작업 지시가 들어오면 아래 두 단계를 분리해서 진행한다.

### 1단계 — 기획자 (Planner)

> 구현하기 전에 반드시 이 단계를 거친다.

- 레포 구조와 관련 파일을 탐색해 현재 상태를 파악한다.
- 모호한 부분이 있으면 **추측하지 않고 Junseo에게 되묻는다.**
- 변경이 필요한 파일 목록, 이유, 순서를 정리해 "작업 계획"으로 제시한다.
- 계획 승인 전까지 파일을 수정하지 않는다.

### 2단계 — 엔지니어 (Executor)

> Junseo가 계획을 승인한 뒤에만 진행한다.

- 승인된 계획을 충실히 구현한다. 범위를 임의로 늘리지 않는다.
- 작업 전 `git pull`로 원격 변경사항 먼저 확인한다.
- 완료 후 변경 파일 경로와 요약을 보고한다.

---

## 레포 작업 기본 절차

```
1. git pull (쓰기 작업 전 필수)
2. 관련 파일 탐색 → 기획자 단계: 계획 제시
3. Junseo 승인
4. 엔지니어 단계: 구현
5. 결과 보고 (변경 파일 경로 포함)
```

---

# Linear 이슈 작성 및 관리 규칙

> DES-224 기반 | DES-224 방향 반영

## 이슈 제목

**핵심 원칙: 목적을 포함하되, 최대한 짧은 명사구**

- 무엇을 했는지 (X) → 왜 필요한지 + 무엇을 하는지 (O)
- **명사구로 마무리** 선호 ("~세팅", "~구축", "~다각화") — 긴 서술형 ("~할 수 있도록") 지양
- 짧게 줄여도 의미 전달이 비슷하면 무조건 짧게
- **프로젝트 내 통일 어휘 사용** — 같은 개념은 항상 같은 단어로. 지엽적인 기술 용어는 제목에 넣지 말 것

```
❌ "CLAUDE.md 계층 구조 설계"              (무엇을 했는지만)
✅ "어느 맥에서든 클로드 지침 일관 적용"    (목적 + 명사구)

❌ "유저 프로필 기반 derivedKeywords 생성 파이프라인 추가"  (지엽 기술 용어)
✅ "프로필 기반 대화가 자연스럽도록 키워드 다각화"          (통일 어휘 + 명사구)

❌ "프롬프트 바꿀 때마다 5분 안에 품질 변화를 확인할 수 있도록"  (너무 긺)
✅ "클로드가 채점 후 사용자가 검수하는 플로우 구축"              (행위자 + 명사구)
```

**프로젝트 통일 어휘 (독서 도우미 기준)**
- 사용자 인풋 / 맥락 / 유저 프로필 → **프로필**
- 메모 → wiki 변환 → **인제스트(Ingest)**
- 질문 추천 → **넛지(Nudge)**
- 지식 저장소 → **위키(Wiki)**
- AI 품질 평가 → **채점 / 합격률**

> 새 프로젝트에서는 해당 프로젝트의 통일 어휘를 먼저 파악하고 제목에 일관 적용.

## 이슈 본문 구조
```
## 배경
이 이슈가 나오게 된 배경.
이 이슈라고 생각하게 만든 다른 의사결정, 작업 진행 상황, 문제의식 등.
(어떤 작업을 하다가, 어떤 결정을 내리다가 이 필요성이 드러났는가)

## 목표
왜 이 이슈가 필요한가

## 작업
구체적으로 무엇을 할 것인가

## 결과
완료 후 실제로 무엇이 달라졌는가 (완료 시 채움)
```

## 이슈 책임 관리 원칙

### 완료 시
- 이슈 상태를 Done으로 변경
- 본문 `## 결과` 섹션에 실제 결과 기록 (무엇을 만들었는지, 어디에 저장됐는지)

### 방향이 바뀌었을 때
- 이슈 본문에 `## 방향 변경 로그` 섹션 추가
- 댓글로도 변경 내용 기록: 언제, 왜 바뀌었는지
- 충돌하는 기존 이슈가 있으면 해당 이슈에도 댓글로 영향 명시

### 새 이슈 생성 시 — 필수 체크리스트

**이슈 생성 전 아래 순서 반드시 실행 (추측 금지):**

1. `list_milestones` 호출 → 마일스톤 목록 확인 (이름 기억에 의존 금지)
2. `## 프로젝트 / 팀 배정` 테이블 참조 → project 이름 확인
3. `save_issue` 호출 시 아래 필드 **모두 포함**:
   - `team` — 팀 이름
   - `project` — 프로젝트 이름
   - `milestone` — list_milestones 결과에서 확인한 정확한 이름
   - `assignee` — 담당자

**누락 허용 안 됨**: project 또는 milestone 빠진 이슈 생성 금지.

- 기존 이슈와 충돌 여부 먼저 확인
- 기존 이슈를 수정하는 방향이면 새 이슈보다 기존 이슈 업데이트 우선
- 충돌하는 이슈가 있으면 `blockedBy` 또는 `relatedTo` 관계 설정

### 수정 내역은 댓글로
- 이슈 완료 후 변경사항이 생기면 본문 수정 + 댓글로 변경 이유 기록
- "언제 무엇을 왜 바꿨는지" 추적 가능하게 유지

## 이슈 마일스톤 배정

이슈 생성 전 반드시:
1. `list_milestones`로 해당 프로젝트 마일스톤 목록 읽기
2. 이슈 목적이 어느 마일스톤 달성에 기여하는지 판단
3. 적합한 마일스톤 없으면 새 마일스톤 제안

판단 기준: "이 이슈가 완료되면 어느 마일스톤이 한 걸음 진전되는가?"

## 프로젝트 시작 시 이슈 생성

새 프로젝트 시작할 때 클로드가 해야 할 것:
1. 마일스톤 생성 (프로젝트 목표 — 왜 이 마일스톤이 필요한지 description에 기록)
2. 주요 작업 단위별 이슈 생성 (제목은 "왜 필요한지" 원칙 적용)
3. 첫 이슈를 In Progress로 설정

## 대화 중 의사결정 기록

작업 중 대화에서 중요한 의사결정이 나오면 즉시 리니어에 반영:
- **방향 전환**: 이슈 댓글 + 본문 `## 방향 변경 로그` 업데이트
- **준서의 인사이트·깨달음**: 관련 이슈 댓글로 기록 ("인사이트: ...")
- **완료된 작업**: 이슈 Done + `## 결과` 섹션 채우기

기준: "나중에 왜 이렇게 됐는지 알 수 있어야 한다"

## 서브이슈

큰 이슈는 서브이슈로 분해. 서브이슈가 모두 Done이어야 부모 이슈 Done 처리.

**기본 규칙: 작업 단위가 2개 이상이면 한 이슈에 몰아넣지 말고 반드시 서브이슈로 쪼갠다. (이슈 생성 시 필수 점검 단계)**

**서브이슈 제목 원칙: "왜 이 서브이슈가 부모 이슈 해결에 필요한지"에 초점**

```
부모: "하루 마무리 회고와 지식 축적이 자동으로 이어지도록"

❌ "회고 포맷 정의"           (무엇을 했는지)
✅ "회고에서 의사결정이 빠지지 않도록 포맷 구조화"  (왜 필요한지)

❌ "wiki ingest 연결"
✅ "회고가 끝나면 위키에 자동 반영되도록"
```

## 프로젝트 / 팀 배정
| 성격 | 팀 | 프로젝트 |
|------|-----|---------|
| 개인 작업 (PKM, 독서, 생산성) | Junseo | 해당 프로젝트 |
| SKT 업무 | SKT | 해당 프로젝트 |
